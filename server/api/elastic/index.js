'use strict';

var express = require('express');
var controller = require('./elastic.controller');

var router = express.Router();

// router.post('/search', controller.search);
// router.post('/search2', controller.search);

module.exports = router;